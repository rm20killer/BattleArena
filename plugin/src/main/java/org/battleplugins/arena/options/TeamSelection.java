package org.battleplugins.arena.options;

public enum TeamSelection {
    /**
     * Teams are randomly selected when the player joins
     * the game.
     */
    RANDOM,
    /**
     * Players can pick their own teams.
     */
    PICK,
    /**
     * No team selection (disabled).
     */
    NONE
}
